package h03;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class H2_2_UnitTest {

  @Test
  public void testZwillingspaare() {
    /*
    World.setSize(10, 10);
    World.setDelay(0);
    World.setVisible(false);

    RobotWithInitialState rob1 = RobotWithInitialState(0, 2, Direction.UP, 10);
    RobotWithInitialState2 rob2 = RobotWithInitialState2(3, 0, Direction.UP, 100);

    assertEquals(rob1.getRelativeNumberOfCoins(), rob2.getRelativeNumberOfCoins());

    rob1.move();
    rob2.move();
    assertEquals(rob1.getRelativeNumberOfCoins(), rob2.getRelativeNumberOfCoins());
    */

    //TODO H2.2
  }
}
